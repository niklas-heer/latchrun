LATCHRUN — THIRD-PARTY LICENSE INVENTORY
======================================

Generated from Cargo.lock with cargo-about 0.9.2 and about.toml.
Includes normal and build dependencies for the union of these release targets:
aarch64-apple-darwin, x86_64-apple-darwin,
aarch64-unknown-linux-gnu, x86_64-unknown-linux-gnu.
Development-only and unrelated platform dependencies are omitted.
Where a crate offers alternative licenses, this inventory selects the license
listed below. This is an inventory, not a legal opinion or a security audit.
Latchrun itself is distributed under the separate LICENSE file.

{{#each licenses}}
-------------------------------------------------------------------------------
{{{name}}} ({{id}})
-------------------------------------------------------------------------------
{{#each used_by}}
Crate: {{crate.name}} {{crate.version}}
{{#if crate.repository}}Source: {{{crate.repository}}}
{{/if}}{{#each crate.authors}}Author: {{{this}}}
{{/each}}{{/each}}
{{{text}}}

{{/each}}
